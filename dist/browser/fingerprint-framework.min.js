(function attachFingerprintFramework(root) {'use strict';const VERSION = '0.1.0'; const SCHEMA_VERSION = 'ff-v1'; const DEFAULT_COLLECTOR_TIMEOUT_MS = 700; const SENSITIVITY_RANK = Object.freeze({ low: 1, medium: 2, high: 3 }); const PROFILE_PRESETS = Object.freeze({ strict: Object.freeze({ maxSensitivity: 'low', includeActive: false, includeUnstable: false }), balanced: Object.freeze({ maxSensitivity: 'medium', includeActive: false, includeUnstable: false }), extended: Object.freeze({ maxSensitivity: 'high', includeActive: true, includeUnstable: true }) }); function createClient(options = {}) { const clientOptions = normalizeClientOptions(options); const collectors = normalizeCollectors(options.collectors || createDefaultCollectors()); const policy = createPolicy(clientOptions.profile, options.policy || {}); return Object.freeze({ version: VERSION, profile: clientOptions.profile, collectors: collectors.map((collector) => collector.id), async identify(context = {}) { return identifyWithCollectors(collectors, policy, clientOptions, context); }, async components(context = {}) { const runtime = createRuntimeContext(clientOptions, context); const collected = await collectComponents(collectors, policy, runtime, clientOptions.collectorTimeoutMs); return collected.map((component) => redactComponent(component, policy)); } }); } async function identifyWithCollectors(collectors, policy, clientOptions, context) { const startedAt = nowMs(); const runtime = createRuntimeContext(clientOptions, context); const requestId = createRequestId(runtime); const createdAt = new Date(runtime.now()).toISOString(); if (policy.requireConsent && !hasConsent(runtime.consent)) { return createBlockedResult({ requestId, createdAt, namespace: clientOptions.namespace, profile: clientOptions.profile, reason: 'consent_required', durationMs: elapsedSince(startedAt) }); } const components = await collectComponents(collectors, policy, runtime, clientOptions.collectorTimeoutMs); const payload = createHashPayload(components, clientOptions.namespace, clientOptions.salt); const confidence = calculateConfidence(components, collectors, policy); const okComponentCount = components.filter((component) => component.status === 'ok').length; const hash = okComponentCount > 0 ? await hashValue(canonicalStringify(payload), runtime) : null; const visitorId = hash ? hash.value : null; const storage = await updateStorageState(clientOptions.storage, clientOptions.storageKey, visitorId, createdAt); return Object.freeze({ visitorId, requestId, namespace: clientOptions.namespace, createdAt, confidence, components: components.map((component) => redactComponent(component, policy)), meta: Object.freeze({ version: VERSION, schemaVersion: SCHEMA_VERSION, profile: clientOptions.profile, durationMs: elapsedSince(startedAt), hashAlgorithm: hash ? hash.algorithm : null, blocked: false, reason: null, storage }) }); } function createBlockedResult(details) { return Object.freeze({ visitorId: null, requestId: details.requestId, namespace: details.namespace, createdAt: details.createdAt, confidence: Object.freeze({ score: 0, level: 'low', entropy: 0, collectedWeight: 0, possibleWeight: 0 }), components: Object.freeze([]), meta: Object.freeze({ version: VERSION, schemaVersion: SCHEMA_VERSION, profile: details.profile, durationMs: details.durationMs, hashAlgorithm: null, blocked: true, reason: details.reason, storage: Object.freeze({ enabled: false, status: 'skipped' }) }) }); } function normalizeClientOptions(options) { const profile = options.profile || 'balanced'; if (!PROFILE_PRESETS[profile]) { throw new TypeError(`Unknown privacy profile: ${profile}`); } const namespace = String(options.namespace || defaultNamespace()); const storage = resolveStorage(options.storage, namespace); return Object.freeze({ profile, namespace, salt: String(options.salt || ''), collectorTimeoutMs: Number.isFinite(options.collectorTimeoutMs) ? Math.max(0, Number(options.collectorTimeoutMs)) : DEFAULT_COLLECTOR_TIMEOUT_MS, storage, storageKey: `fingerprint-framework:${namespace}:state`, consent: options.consent || null, now: typeof options.now === 'function' ? options.now : Date.now }); } function createPolicy(profile = 'balanced', overrides = {}) { if (!PROFILE_PRESETS[profile]) { throw new TypeError(`Unknown privacy profile: ${profile}`); } const preset = PROFILE_PRESETS[profile]; const maxSensitivity = overrides.maxSensitivity || preset.maxSensitivity; if (!SENSITIVITY_RANK[maxSensitivity]) { throw new TypeError(`Unknown sensitivity: ${maxSensitivity}`); } return Object.freeze({ profile, requireConsent: Boolean(overrides.requireConsent), redactValues: Boolean(overrides.redactValues), maxSensitivity, includeActive: typeof overrides.includeActive === 'boolean' ? overrides.includeActive : preset.includeActive, includeUnstable: typeof overrides.includeUnstable === 'boolean' ? overrides.includeUnstable : preset.includeUnstable, allowCollectors: toFrozenSet(overrides.allowCollectors), denyCollectors: toFrozenSet(overrides.denyCollectors), allowCategories: toFrozenSet(overrides.allowCategories), denyCategories: toFrozenSet(overrides.denyCategories) }); } function createCollector(definition) { if (!definition || typeof definition !== 'object') { throw new TypeError('Collector definition must be an object.'); } if (!definition.id || typeof definition.id !== 'string') { throw new TypeError('Collector id must be a non-empty string.'); } if (typeof definition.collect !== 'function') { throw new TypeError(`Collector ${definition.id} must provide collect(context).`); } const sensitivity = definition.sensitivity || 'low'; if (!SENSITIVITY_RANK[sensitivity]) { throw new TypeError(`Collector ${definition.id} has unknown sensitivity: ${sensitivity}`); } return Object.freeze({ id: definition.id, version: String(definition.version || '1'), category: String(definition.category || 'custom'), sensitivity, mode: definition.mode === 'active' ? 'active' : 'passive', stability: definition.stability || 'stable', weight: Number.isFinite(definition.weight) ? Math.max(0, Number(definition.weight)) : 1, collect: definition.collect }); } function createDefaultCollectors() { return [ createBrowserRuntimeCollector(), createNodeRuntimeCollector(), createLocaleCollector(), createTimezoneCollector(), createScreenCollector(), createHardwareCollector(), createStorageCapabilitiesCollector(), createWebglCollector(), createCanvasCollector() ]; } function createBrowserCollectorPack() { return createDefaultCollectors().filter((collector) => collector.id !== 'runtime.node'); } function createBrowserRuntimeCollector() { return createCollector({ id: 'runtime.browser', version: '1', category: 'runtime', sensitivity: 'medium', mode: 'passive', stability: 'stable', weight: 1.4, collect(context) { const navigatorRef = context.navigator; if (!navigatorRef) { return null; } const userAgentData = navigatorRef.userAgentData ? { brands: normalizeBrands(navigatorRef.userAgentData.brands), mobile: Boolean(navigatorRef.userAgentData.mobile), platform: navigatorRef.userAgentData.platform || null } : null; return { userAgent: navigatorRef.userAgent || null, platform: navigatorRef.platform || null, vendor: navigatorRef.vendor || null, productSub: navigatorRef.productSub || null, webdriver: navigatorRef.webdriver === true, userAgentData }; } }); } function createNodeRuntimeCollector() { return createCollector({ id: 'runtime.node', version: '1', category: 'runtime', sensitivity: 'low', mode: 'passive', stability: 'stable', weight: 0.4, collect() { if (typeof process === 'undefined' || !process.versions || !process.versions.node) { return null; } return { node: process.versions.node, platform: process.platform, arch: process.arch }; } }); } function createLocaleCollector() { return createCollector({ id: 'locale', version: '1', category: 'locale', sensitivity: 'low', mode: 'passive', stability: 'stable', weight: 0.8, collect(context) { const navigatorRef = context.navigator; const intlOptions = typeof Intl !== 'undefined' && Intl.DateTimeFormat ? Intl.DateTimeFormat().resolvedOptions() : {}; return { language: navigatorRef ? navigatorRef.language || null : null, languages: navigatorRef && Array.isArray(navigatorRef.languages) ? navigatorRef.languages.slice(0, 10) : [], locale: intlOptions.locale || null }; } }); } function createTimezoneCollector() { return createCollector({ id: 'timezone', version: '1', category: 'locale', sensitivity: 'medium', mode: 'passive', stability: 'stable', weight: 0.9, collect() { const intlOptions = typeof Intl !== 'undefined' && Intl.DateTimeFormat ? Intl.DateTimeFormat().resolvedOptions() : {}; return { timeZone: intlOptions.timeZone || null, offsetMinutes: new Date().getTimezoneOffset() }; } }); } function createScreenCollector() { return createCollector({ id: 'screen.metrics', version: '1', category: 'display', sensitivity: 'medium', mode: 'passive', stability: 'stable', weight: 1.1, collect(context) { const screenRef = context.screen; if (!screenRef) { return null; } return { width: safeNumber(screenRef.width), height: safeNumber(screenRef.height), availWidth: safeNumber(screenRef.availWidth), availHeight: safeNumber(screenRef.availHeight), colorDepth: safeNumber(screenRef.colorDepth), pixelDepth: safeNumber(screenRef.pixelDepth), devicePixelRatio: safeNumber(context.global.devicePixelRatio) }; } }); } function createHardwareCollector() { return createCollector({ id: 'hardware', version: '1', category: 'hardware', sensitivity: 'medium', mode: 'passive', stability: 'stable', weight: 1, collect(context) { const navigatorRef = context.navigator; if (!navigatorRef) { return null; } return { hardwareConcurrency: safeNumber(navigatorRef.hardwareConcurrency), deviceMemory: safeNumber(navigatorRef.deviceMemory), maxTouchPoints: safeNumber(navigatorRef.maxTouchPoints) }; } }); } function createStorageCapabilitiesCollector() { return createCollector({ id: 'storage.capabilities', version: '1', category: 'storage', sensitivity: 'low', mode: 'passive', stability: 'stable', weight: 0.5, collect(context) { const navigatorRef = context.navigator; return { cookieEnabled: navigatorRef ? Boolean(navigatorRef.cookieEnabled) : null, doNotTrack: navigatorRef ? navigatorRef.doNotTrack || null : null, localStorage: canUseStorage(context.global, 'localStorage'), sessionStorage: canUseStorage(context.global, 'sessionStorage') }; } }); } function createWebglCollector() { return createCollector({ id: 'webgl.renderer', version: '1', category: 'graphics', sensitivity: 'high', mode: 'active', stability: 'stable', weight: 1.6, collect(context) { const documentRef = context.document; if (!documentRef || typeof documentRef.createElement !== 'function') { return null; } const canvas = documentRef.createElement('canvas'); const gl = canvas.getContext && (canvas.getContext('webgl') || canvas.getContext('experimental-webgl')); if (!gl) { return null; } const debugInfo = gl.getExtension ? gl.getExtension('WEBGL_debug_renderer_info') : null; return { vendor: getGlParameter(gl, gl.VENDOR), renderer: getGlParameter(gl, gl.RENDERER), version: getGlParameter(gl, gl.VERSION), shadingLanguageVersion: getGlParameter(gl, gl.SHADING_LANGUAGE_VERSION), unmaskedVendor: debugInfo ? getGlParameter(gl, debugInfo.UNMASKED_VENDOR_WEBGL) : null, unmaskedRenderer: debugInfo ? getGlParameter(gl, debugInfo.UNMASKED_RENDERER_WEBGL) : null }; } }); } function createCanvasCollector() { return createCollector({ id: 'canvas.checksum', version: '1', category: 'graphics', sensitivity: 'high', mode: 'active', stability: 'stable', weight: 1.4, collect(context) { const documentRef = context.document; if (!documentRef || typeof documentRef.createElement !== 'function') { return null; } const canvas = documentRef.createElement('canvas'); canvas.width = 240; canvas.height = 80; const canvasContext = canvas.getContext && canvas.getContext('2d'); if (!canvasContext || typeof canvas.toDataURL !== 'function') { return null; } canvasContext.textBaseline = 'top'; canvasContext.font = '16px Arial'; canvasContext.fillStyle = '#f60'; canvasContext.fillRect(8, 8, 96, 28); canvasContext.fillStyle = '#069'; canvasContext.fillText('Fingerprint Framework 0.1', 12, 14); canvasContext.globalCompositeOperation = 'multiply'; canvasContext.fillStyle = 'rgba(102, 204, 0, 0.65)'; canvasContext.beginPath(); canvasContext.arc(80, 42, 24, 0, Math.PI * 2, true); canvasContext.closePath(); canvasContext.fill(); const dataUrl = canvas.toDataURL(); return { length: dataUrl.length, checksum: checksumString(dataUrl) }; } }); } function normalizeCollectors(collectors) { if (!Array.isArray(collectors)) { throw new TypeError('collectors must be an array.'); } const seen = new Set(); return collectors.map((collector) => { const normalized = collector && typeof collector.collect === 'function' && collector.id ? createCollector(collector) : createCollector(collector); if (seen.has(normalized.id)) { throw new TypeError(`Duplicate collector id: ${normalized.id}`); } seen.add(normalized.id); return normalized; }); } async function collectComponents(collectors, policy, runtime, timeoutMs) { const tasks = collectors.map((collector) => { if (!isCollectorAllowed(collector, policy)) { return Promise.resolve(createSkippedComponent(collector, 'policy_denied')); } return collectOneComponent(collector, runtime, timeoutMs); }); const components = await Promise.all(tasks); return components.sort((left, right) => left.id.localeCompare(right.id)); } async function collectOneComponent(collector, runtime, timeoutMs) { const startedAt = nowMs(); try { const value = await withTimeout(Promise.resolve().then(() => collector.collect(runtime)), timeoutMs, collector.id); const canonicalValue = toCanonical(value); const status = canonicalValue === null ? 'empty' : 'ok'; return freezeComponent({ id: collector.id, version: collector.version, category: collector.category, sensitivity: collector.sensitivity, mode: collector.mode, stability: collector.stability, weight: collector.weight, status, value: canonicalValue, durationMs: elapsedSince(startedAt), error: null }); } catch (error) { return freezeComponent({ id: collector.id, version: collector.version, category: collector.category, sensitivity: collector.sensitivity, mode: collector.mode, stability: collector.stability, weight: collector.weight, status: error && error.code === 'collector_timeout' ? 'timeout' : 'error', value: null, durationMs: elapsedSince(startedAt), error: normalizeError(error) }); } } function createSkippedComponent(collector, reason) { return freezeComponent({ id: collector.id, version: collector.version, category: collector.category, sensitivity: collector.sensitivity, mode: collector.mode, stability: collector.stability, weight: collector.weight, status: 'skipped', value: null, durationMs: 0, error: Object.freeze({ code: reason, message: reason }) }); } function freezeComponent(component) { return Object.freeze({ id: component.id, version: component.version, category: component.category, sensitivity: component.sensitivity, mode: component.mode, stability: component.stability, weight: component.weight, status: component.status, value: component.value, durationMs: component.durationMs, error: component.error }); } function createHashPayload(components, namespace, salt) { const values = {}; for (const component of components) { if (component.status === 'ok') { values[component.id] = { version: component.version, value: component.value }; } } return { schemaVersion: SCHEMA_VERSION, namespace, salt, values }; } function calculateConfidence(components, collectors, policy) { let possibleWeight = 0; let collectedWeight = 0; let entropy = 0; const allowedCollectorIds = new Set( collectors.filter((collector) => isCollectorAllowed(collector, policy)).map((collector) => collector.id) ); for (const collector of collectors) { if (allowedCollectorIds.has(collector.id)) { possibleWeight += collector.weight; } } for (const component of components) { if (component.status !== 'ok') { continue; } collectedWeight += component.weight; entropy += component.weight * SENSITIVITY_RANK[component.sensitivity]; } const score = possibleWeight > 0 ? round(clamp(collectedWeight / possibleWeight, 0, 1), 3) : 0; return Object.freeze({ score, level: score >= 0.75 ? 'high' : score >= 0.45 ? 'medium' : 'low', entropy: round(entropy, 3), collectedWeight: round(collectedWeight, 3), possibleWeight: round(possibleWeight, 3) }); } function isCollectorAllowed(collector, policy) { if (policy.denyCollectors.has(collector.id)) { return false; } if (policy.allowCollectors.size > 0 && !policy.allowCollectors.has(collector.id)) { return false; } if (policy.denyCategories.has(collector.category)) { return false; } if (policy.allowCategories.size > 0 && !policy.allowCategories.has(collector.category)) { return false; } if (SENSITIVITY_RANK[collector.sensitivity] > SENSITIVITY_RANK[policy.maxSensitivity]) { return false; } if (collector.mode === 'active' && !policy.includeActive) { return false; } if (collector.stability === 'volatile' && !policy.includeUnstable) { return false; } return true; } function redactComponent(component, policy) { if (!policy.redactValues || component.status !== 'ok') { return component; } return Object.freeze({ ...component, value: '[redacted]' }); } function createRuntimeContext(options, context = {}) { const globalRef = context.global || getGlobal(); return Object.freeze({ global: globalRef, window: context.window || globalRef.window || globalRef, document: context.document || globalRef.document || null, navigator: context.navigator || globalRef.navigator || null, screen: context.screen || globalRef.screen || null, crypto: context.crypto || globalRef.crypto || null, consent: context.consent || options.consent || null, now: typeof context.now === 'function' ? context.now : options.now }); } async function hashValue(value, runtime = {}) { const text = String(value); const bytes = encodeText(text); const cryptoRef = runtime.crypto || getGlobal().crypto || null; if (cryptoRef && cryptoRef.subtle && typeof cryptoRef.subtle.digest === 'function') { const digest = await cryptoRef.subtle.digest('SHA-256', bytes); return Object.freeze({ algorithm: 'sha256:webcrypto', value: bytesToHex(new Uint8Array(digest)) }); } try { const nodeCrypto = await import('node:crypto'); const value = nodeCrypto.createHash('sha256').update(text).digest('hex'); return Object.freeze({ algorithm: 'sha256:node', value }); } catch (_error) { return Object.freeze({ algorithm: 'fnv1a64:fallback', value: fnv1a64Hex(text) }); } } function canonicalStringify(value) { return JSON.stringify(toCanonical(value)); } function toCanonical(value) { if (value === null) { return null; } const valueType = typeof value; if (valueType === 'string' || valueType === 'boolean') { return value; } if (valueType === 'number') { return Number.isFinite(value) ? value : null; } if (valueType === 'bigint') { return value.toString(); } if (value instanceof Date) { return Number.isNaN(value.getTime()) ? null : value.toISOString(); } if (Array.isArray(value)) { return value.map((item) => toCanonical(item)); } if (valueType === 'undefined' || valueType === 'function' || valueType === 'symbol') { return undefined; } if (valueType === 'object') { const output = {}; const keys = Object.keys(value).sort(); for (const key of keys) { const normalized = toCanonical(value[key]); if (typeof normalized !== 'undefined') { output[key] = normalized; } } return output; } return null; } function resolveStorage(storageOption, namespace) { if (!storageOption) { return null; } if (storageOption === 'local') { const globalRef = getGlobal(); if (!canUseStorage(globalRef, 'localStorage')) { return null; } return Object.freeze({ type: 'localStorage', async get(key) { return globalRef.localStorage.getItem(key); }, async set(key, value) { globalRef.localStorage.setItem(key, value); } }); } if (storageOption && typeof storageOption.get === 'function' && typeof storageOption.set === 'function') { return Object.freeze({ type: storageOption.type || `custom:${namespace}`, get: storageOption.get.bind(storageOption), set: storageOption.set.bind(storageOption) }); } throw new TypeError('storage must be false, "local", or an object with get/set methods.'); } async function updateStorageState(storage, key, visitorId, createdAt) { if (!storage || !visitorId) { return Object.freeze({ enabled: Boolean(storage), status: visitorId ? 'disabled' : 'skipped' }); } try { const previousRaw = await storage.get(key); const previous = previousRaw ? JSON.parse(previousRaw) : null; const next = { visitorId, firstSeenAt: previous && previous.visitorId === visitorId ? previous.firstSeenAt : createdAt, lastSeenAt: createdAt, seenCount: previous && previous.visitorId === visitorId ? Number(previous.seenCount || 0) + 1 : 1 }; await storage.set(key, JSON.stringify(next)); return Object.freeze({ enabled: true, type: storage.type, status: previous && previous.visitorId === visitorId ? 'updated' : 'created', firstSeenAt: next.firstSeenAt, seenCount: next.seenCount }); } catch (error) { return Object.freeze({ enabled: true, type: storage.type, status: 'error', error: normalizeError(error) }); } } function withTimeout(promise, timeoutMs, collectorId) { const globalRef = getGlobal(); const setTimer = typeof setTimeout === 'function' ? setTimeout : globalRef.setTimeout; const clearTimer = typeof clearTimeout === 'function' ? clearTimeout : globalRef.clearTimeout; if (!timeoutMs || typeof setTimer !== 'function' || typeof clearTimer !== 'function') { return promise; } let timeoutId; const timeout = new Promise((_resolve, reject) => { timeoutId = setTimer(() => { const error = new Error(`Collector timed out: ${collectorId}`); error.code = 'collector_timeout'; reject(error); }, timeoutMs); }); return Promise.race([promise, timeout]).finally(() => clearTimer(timeoutId)); } function hasConsent(consent) { if (consent === true) { return true; } if (!consent || typeof consent !== 'object') { return false; } return consent.granted === true; } function defaultNamespace() { const globalRef = getGlobal(); const locationRef = globalRef.location; if (locationRef && locationRef.hostname) { return locationRef.hostname; } return 'default'; } function toFrozenSet(value) { if (!value) { return Object.freeze(new Set()); } if (!Array.isArray(value)) { throw new TypeError('Policy allow/deny lists must be arrays.'); } return Object.freeze(new Set(value.map(String))); } function normalizeBrands(brands) { if (!Array.isArray(brands)) { return []; } return brands .map((brand) => ({ brand: brand.brand || null, version: brand.version || null })) .sort((left, right) => String(left.brand).localeCompare(String(right.brand))); } function safeNumber(value) { return Number.isFinite(value) ? Number(value) : null; } function getGlParameter(gl, parameter) { try { const value = gl.getParameter(parameter); return typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean' ? value : null; } catch (_error) { return null; } } function canUseStorage(globalRef, key) { try { const storage = globalRef && globalRef[key]; if (!storage || typeof storage.setItem !== 'function' || typeof storage.removeItem !== 'function') { return false; } const testKey = '__fingerprint_framework_test__'; storage.setItem(testKey, '1'); storage.removeItem(testKey); return true; } catch (_error) { return false; } } function encodeText(text) { if (typeof TextEncoder !== 'undefined') { return new TextEncoder().encode(text); } const bytes = new Uint8Array(text.length); for (let index = 0; index < text.length; index += 1) { bytes[index] = text.charCodeAt(index) & 0xff; } return bytes; } function bytesToHex(bytes) { return Array.from(bytes, (byte) => byte.toString(16).padStart(2, '0')).join(''); } function fnv1a64Hex(text) { let hash = 0xcbf29ce484222325n; const prime = 0x100000001b3n; for (let index = 0; index < text.length; index += 1) { hash ^= BigInt(text.charCodeAt(index)); hash = BigInt.asUintN(64, hash * prime); } return hash.toString(16).padStart(16, '0'); } function checksumString(text) { let first = 0xdeadbeef; let second = 0x41c6ce57; for (let index = 0; index < text.length; index += 1) { const code = text.charCodeAt(index); first = Math.imul(first ^ code, 2654435761); second = Math.imul(second ^ code, 1597334677); } first = Math.imul(first ^ (first >>> 16), 2246822507) ^ Math.imul(second ^ (second >>> 13), 3266489909); second = Math.imul(second ^ (second >>> 16), 2246822507) ^ Math.imul(first ^ (first >>> 13), 3266489909); return `${(second >>> 0).toString(16).padStart(8, '0')}${(first >>> 0).toString(16).padStart(8, '0')}`; } function createRequestId(runtime) { const cryptoRef = runtime.crypto || getGlobal().crypto || null; if (cryptoRef && typeof cryptoRef.randomUUID === 'function') { return cryptoRef.randomUUID(); } return `req_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 12)}`; } function normalizeError(error) { if (!error) { return Object.freeze({ code: 'unknown', message: 'Unknown error' }); } return Object.freeze({ code: error.code || error.name || 'error', message: error.message || String(error) }); } function getGlobal() { if (typeof globalThis !== 'undefined') { return globalThis; } if (typeof window !== 'undefined') { return window; } return {}; } function nowMs() { if (typeof performance !== 'undefined' && typeof performance.now === 'function') { return performance.now(); } return Date.now(); } function elapsedSince(startedAt) { return round(Math.max(0, nowMs() - startedAt), 3); } function clamp(value, min, max) { return Math.min(max, Math.max(min, value)); } function round(value, decimals) { const factor = 10 ** decimals; return Math.round(value * factor) / factor; } const api = Object.freeze({VERSION,PROFILE_PRESETS,canonicalStringify,createBrowserCollectorPack,createClient,createCollector,createDefaultCollectors,createPolicy,hashValue});root.FingerprintFramework = api;if (typeof module !== 'undefined' && module.exports) {module.exports = api;}})(typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : this);